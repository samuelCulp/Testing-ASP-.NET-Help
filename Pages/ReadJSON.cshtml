@* @page
@model Testing_ASP_.NET.Pages.ReadJSONModel
@{
    ViewData["Title"] = "Edit Naming Conventions";
}

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="~/css/styles.css">
    <title>Naming Conventions</title>
</head>
<body>
    <h2>Naming Conventions</h2>

    @if (Model.NamingConventions.Count > 0)
    {
        @foreach (var category in Model.NamingConventions)
        {
            <h3>@category.Key</h3>
            <div class="table-container">
                <form method="post">
                    <input type="hidden" name="Category" value="@category.Key" />
                    <table>
                        <thead>
                            <tr>
                                <th>Convention</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < category.Value.Count; i++)
                            {
                                <tr>
                                    <td>
                                        <input type="text" name="Conventions" value="@category.Value[i]" />
                                    </td>
                                    <td>
                                        <button type="submit" name="RemoveIndex" value="@i">Remove</button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <button type="button" class="add-item">Add New Item</button>
                    <button type="submit">Save Changes</button>
                </form>
            </div>
        }
    }
    else
    {
        <p>No naming conventions found.</p>
    }

    <script>
        document.querySelectorAll('.add-item').forEach(button => {
            button.addEventListener('click', function () {
                const tbody = this.closest('form').querySelector('tbody');
                const newRow = document.createElement('tr');
                newRow.innerHTML = `
                                    <td><input type="text" name="Conventions" value="" /></td>
                                    <td><button type="submit">Remove</button></td>
                                `;
                tbody.appendChild(newRow);
            });
        });
    </script>
</body>
</html>
 *@


 @page
@model Testing_ASP_.NET.Pages.ReadJSONModel
@{
    ViewData["Title"] = "Edit Naming Conventions";
}

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="~/css/styles.css">
    <title>Naming Conventions</title>
</head>
<body>
    <h2>Naming Conventions</h2>

    @if (Model.NamingConventions.Count > 0)
    {
        @foreach (var category in Model.NamingConventions)
        {
            <h3>@category.Key</h3>
            <div class="table-container">
                <form method="post">
                    <input type="hidden" name="Category" value="@category.Key" />
                    <table>
                        <thead>
                            <tr>
                                <th>Convention</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < category.Value.Count; i++)
                            {
                                <tr>
                                    <td>
                                        <input type="text" name="Conventions" value="@category.Value[i]" />
                                    </td>
                                    <td>
                                        <button type="submit" name="RemoveIndex" value="@i">Remove</button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <button type="button" class="add-item">Add New Item</button>
                    <button type="submit">Save Changes</button>
                </form>
            </div>
        }
    }
    else
    {
        <p>No naming conventions found.</p>
    }

    <script>
        document.querySelectorAll('.add-item').forEach(button => {
            button.addEventListener('click', function () {
                const tbody = this.closest('form').querySelector('tbody');
                const newRow = document.createElement('tr');
                newRow.innerHTML = `
                            <td><input type="text" name="Conventions" value="" /></td>
                            <td><button type="submit">Remove</button></td>
                        `;
                tbody.appendChild(newRow);
            });
        });
    </script>
</body>
</html>
